import { ThemedText, ThemedView } from "@/components/ui"

const ProfileIndex = () => {
  return (
    <ThemedView style={{flex:1}}>
      <ThemedText>
        hi ther, this is my profile
      </ThemedText>
    </ThemedView>
  )
}

export default ProfileIndex

